<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Domain Manager" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script>
      (function() {
        var t = localStorage.getItem('theme');
        if (!t) t = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
        if (t === 'dark') document.documentElement.classList.add('dark');
      })();
    </script>
  </head>
  <body class="min-h-screen">
    <main class="<%= current_user.present? ? 'container mx-auto' : '' %>">
      <% if current_user.present? %>
        <nav class="nav-bar">
          <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
              <div class="flex items-center">
                <div class="flex-shrink-0 flex items-center">
                  <svg class="h-8 w-8 mr-3" style="color: var(--color-accent-primary)" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
                    <circle cx="12" cy="12" r="9"/>
                    <ellipse cx="12" cy="12" rx="3.5" ry="9"/>
                    <line x1="3" y1="9.5" x2="21" y2="9.5"/>
                    <line x1="3" y1="14.5" x2="21" y2="14.5"/>
                  </svg>
                  <span class="font-bold text-xl" style="color: var(--color-text-primary)">Domain Manager</span>
                </div>
              </div>

              <div class="flex items-center space-x-6">
                <%= link_to "DNS Zones", dns_zones_path, class: "nav-link" %>
                <%= link_to "API Tokens", api_tokens_path, class: "nav-link" %>
                <% if current_user.admin? %>
                  <%= link_to "Users", users_path, class: "nav-link" %>
                <% end %>

                <div class="flex items-center space-x-3 ml-4 pl-4" style="border-left: 1px solid var(--color-border-primary)">
                  <span class="text-sm" style="color: var(--color-text-tertiary)">
                    <%= current_user.name %>
                  </span>
                  <div data-controller="theme">
                    <button class="theme-toggle" data-action="click->theme#toggle" type="button" aria-label="Toggle theme">
                      <svg data-theme-target="icon" class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
                      </svg>
                    </button>
                  </div>
                  <%= link_to "Sign Out", logout_path, method: :delete, class: "btn btn-danger btn-sm" %>
                </div>
              </div>
            </div>
          </div>
        </nav>

        <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
          <% if notice.present? %>
            <div class="flash-notice flash-success">
              <svg class="h-5 w-5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
              <%= notice %>
            </div>
          <% end %>
          <% if alert.present? %>
            <div class="flash-notice flash-alert">
              <svg class="h-5 w-5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z"/>
              </svg>
              <%= alert %>
            </div>
          <% end %>
          <%= yield %>
        </div>
      <% else %>
        <%= yield %>
      <% end %>
    </main>
  </body>
</html>
